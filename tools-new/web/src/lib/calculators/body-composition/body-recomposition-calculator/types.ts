export type Input = {
  /** Body weight in kilograms */
  weightKg: number;

  /** Body fat percentage (0–100) */
  bodyFatPercent: number;

  /** Target body fat percentage (0–100) */
  targetBodyFatPercent: number;
};
